import { HoverCard } from '@mantine/core';
import styled from 'styled-components';

export const HoverCardDropdown = styled(HoverCard.Dropdown)`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  border: 0;
  background-color: transparent;
`;
